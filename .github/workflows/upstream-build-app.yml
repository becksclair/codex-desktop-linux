name: Upstream Build App

# This workflow is intentionally disabled.
# Keep it manual-only so GitHub Actions has no automatic schedule, push, or PR trigger.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Upstream Build App disabled
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: echo "Upstream Build App is disabled for this repository. Restore the previous workflow from git history to re-enable it."
